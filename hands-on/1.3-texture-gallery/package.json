{
  "name": "1.3-texture-gallery",
  "version": "1.0.0",
  "description": "WebGL Texture Gallery using TypeScript",
  "scripts": {
    "dev": "concurrently -k 'tsc --watch'",
    "start:dev": "concurrently -k 'pnpm dev' 'lite-server .'"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "lite-server": "^2.6.1",
    "typescript": "^6.0.3"
  }
}
